# Copied by docker-compose.yml for its own ${VAR} substitution (not injected
# into the container itself - see librarytools-auth.env.example for that).

TZ=Europe/Stockholm
DOMAIN_NAME=apps.lib.kth.se
# ref|main - selects the ghcr.io image tag pushed by CI.
REPO_TYPE=main
